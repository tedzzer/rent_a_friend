<h1>My lovely firends 🍌</h1>

<ul>
<% @friends.each do |friend| %>
<li><%= link_to friend.description, friend_path(friend) %> -- <%= link_to "Delete", dell_path(friend),
                                                  method: :delete,
                                                  data: { confirm: "Are you sure?" } %> </li>
</ul>

  <% if policy(friend).edit? %>
    <%= link_to "Update", edit_friend_path(friend) %>
  <% end %>
  <%# WILL HAVE TO CHNAGE FRIEND.DECRIPTION INTO FRIENDS.NAME %>


<%end %>

  <% if policy(Friend).create? %>
    <%= link_to "New friend", new_friend_path %>
  <% end %>
